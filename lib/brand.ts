/**
 * Brand identity — single source of truth.
 *
 * The product name is NOT finalized. Reference these constants everywhere
 * (sidebar, onboarding, metadata, page titles) so a rename is a one-line change.
 */
export const BRAND_NAME = "resolv.ai";
export const BRAND_TAGLINE = "AI Helpdesk";
