import { Users } from "lucide-react";
import { ComingSoon } from "@/components/shared/coming-soon";

export default function ContactsPage() {
  return (
    <ComingSoon
      icon={Users}
      title="Contacts"
      description="Unified contact profiles across every channel, with conversation history and Shopify customer data."
    />
  );
}
